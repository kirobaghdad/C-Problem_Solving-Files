#include <iostream>
#include <algorithm>
#include <map>
#include <vector>
#include <iomanip>
#include <string>
#include <limits>
#include <queue>
#include <stack>
#include <math.h>

using namespace std;

// naming var in ps
int main()
{
    int t;
    cin >> t;
    int n, c, q;
    string str;
    int l, r;
    int k;
    vector<char> ans;
    char in;

    for (int i = 0; i < t; i++)
    {
        cin >> n >> c >> q;
        cin >> str;
    }

    for (int i = 0; i < ans.size(); i++)
        cout << ans[i] << endl;
}
